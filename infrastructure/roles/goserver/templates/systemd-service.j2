[Unit]
Description=Go ASAPP Server
After=network-online.target

[Service]
User=root
ExecStart={{ server_root }}/{{ unique_server_dir_name }}/go-server {{ pool_server_next_port }}
Restart=always

[Install]
WantedBy=multi-user.target
